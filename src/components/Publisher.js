import React from "react";
import ReactDOM from "react-dom";
import Browser from "./Browser";
const publisher = (props) => {
  return <div>Publisher is initiated!</div>;
};
publisher.Filelist = Browser;
export default publisher;
